export function CommentSection({ articleId, commentCount }: { articleId: string; commentCount: number }) {
  return <div className="py-4 text-gray-400 text-sm">댓글 영역 (구현 예정) - {commentCount}개</div>
}
